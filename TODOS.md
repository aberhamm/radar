<!-- /autoplan restore point: /c/Users/matthew.aberham/.gstack/projects/repo-audit-delivery-agent/main-autoplan-restore-20260403-054236.md -->
# TODOs — repo-audit-delivery-agent

Generated by /plan-eng-review on 2026-03-31. Track what's deferred.

## Demo Blockers (do these)

- [x] **Chunk 0: Pi API spike** — Pi IS available (`@mariozechner/pi-agent-core` v0.64.0). Migrated from DirectLoopRunner to Pi Agent. See `docs/pi-api-notes.md`.
- [x] **Chunk 0: Portkey verification** — Sonnet 4.6 (`us.anthropic.claude-sonnet-4-6`) and Haiku 4.5 (`us.anthropic.claude-haiku-4-5-20251001-v1:0`) confirmed working via Portkey gateway. Tool calling works. Finish reasons: `end_turn`, `tool_use`.
- [x] **Chunk 0: Portkey cache token fields** — NOT surfaced. cachedTokens defaults to 0. (Added by eng review #2)
- [x] **Chunk 0: Pi mid-loop interruption** — Handled via Pi's `beforeToolCall`/`afterToolCall` hooks + `agent.abort()`. (Added by eng review #2)

## Pre-Demo (before first presentation)

- [x] **Model pricing config** — `src/config/model-pricing.json` with Bedrock per-token prices for Sonnet 4.6, Haiku 4.5, Opus 4.6. Cost estimates live in RunMetrics. Falls back to defaults if file missing.
- [x] **Pre-generated demo fixtures** — `scripts/generate-fixtures.sh` runs agent against both target repos and saves to `output/demo-fixtures/`. Regenerate before each demo.

## Demo Runbook

Before each demo:
1. **Verify env**: `source .env && echo $PORTKEY_API_KEY | head -c5` — should print first 5 chars
2. **Quick smoke test**: `npx tsx src/index.ts --help` or `npx tsx src/index.ts tools` — should list 23 tools
3. **Pre-generate fixtures** (if not cached): `bash scripts/generate-fixtures.sh` — runs both repos, ~8 min, ~$2-4 (dual-model)
4. **Second repo** (optional): `npx tsx src/index.ts analyze --repo tmp/optimizely-saas --verbose`
5. **Comparison** (optional): `npx tsx src/index.ts compare tmp/sitecore-xmcloud tmp/optimizely-saas`
6. **Verify outputs**: Check `output/` for fresh brief, scorecard, findings, investigation log
7. **Dashboard** (if built): `pnpm dashboard` — starts Next.js UI and opens browser
8. **GitHub hook** (if demoing): `gh auth status` then add `--github-output` flag

Demo talking points: 11 findings in ~8 min, ~$0.74/run (dual-model, 37% on Haiku), 45 tool calls default, zero hardcoded pipeline, Sonnet investigates then agent calls switch_to_fast_model and Haiku writes.

## Post-Demo (not blocking)

- [x] **Secret redaction** — Implemented in `src/agent/redaction.ts`. Regex-based redaction for KEY, SECRET, TOKEN, PASSWORD patterns applied in runner.ts afterToolCall before results enter LLM context or investigation log.
- [x] **Expanded secret redaction patterns** — Added: AWS access keys (AKIA/ASIA/AROA/AIDA), connection strings (jdbc/mongodb/postgres/mysql/redis/amqp), Bearer tokens, PEM private keys. 20 tests passing.
- [x] **Optimizely rules** — Expanded `platform-optimizely.md` with Visual Builder, Content Graph, @remkoj ecosystem, env vars, package reference table, 16 issue patterns.
- [ ] **Web search / fetch_url** — Currently stubbed. Implement with a real search API when needed for production use.
- [x] **analyze_middleware** — Implemented in `src/tools/analysis/analyzeMiddleware.ts`, registered in piToolAdapter, classified in concurrency.ts, 2 tests passing.
- [x] **E2e tests** — Full agent loop against fixture repo in `test/e2e/`. Asserts sections populated, scored scorecard, evidence-backed findings, output files written.
- [x] **CLI polish** — Renamed to `radar`, added descriptive help text with examples, all flags documented.
- [x] **HTML investigation log** — `src/output/investigationHtml.ts` renders collapsible HTML with scorecard, stats, and per-step details. Wired into runner.ts writeOutputFiles.
- [x] **JSON export** — `FullExport` schema with complete metadata (terminationReason, toolCallsUsed, toolCallBudget). CLI `--export` flag outputs full JSON to stdout.
- [x] **Prompt injection defense** — `src/agent/contextBoundary.ts` with boundary wrapping, pattern detection, and `sanitizeToolOutput()`. Wired into runner.ts afterToolCall. 12 injection patterns covered.
- [x] **CI/CD goal type** — `goal-ci-check.md` rules (3 categories: deps, security, config). Compact `ciComment.ts` renderer for PR comments. Wired into GitHub hook.
- [x] **Intent-based model switch** — Implemented as `switch_to_fast_model` tool. Agent calls it when investigation is complete, switching to Haiku for the writing phase. Replaces the old midpoint-based switch which was timing-dependent and unreliable.
- [x] **Auto-detect app roots** — `src/tools/analysis/detectAppRoots.ts` scans for package.json files, classifies by framework, detects monorepo tooling. Registered as `detect_app_roots` tool.

## Claude Code Patterns to Adopt

Sourced from `C:/_projects/claude-code-main` analysis on 2026-04-02. Patterns that would improve reliability, cost, and context management.

### High Value

- [x] **Tool result disk overflow + smart preview** — Per-tool limits in `piToolAdapter.ts` with `spillAndTruncate()` to tmpdir. grep_pattern: 20K, read_file: 65K, fetch_url: 100K, etc.
- [x] **Tiered context compression** — 3-tier compression in runner.ts: recent (10 msgs full), mid-age (15 msgs at 600 chars), old (120 chars). Cached by toolCallId.
- [x] **Tool concurrency partitioning** — `src/tools/concurrency.ts` with `StatefulToolMutex`. Read-only tools (16) run fully parallel, stateful tools (`record_finding`, `assemble_output`, `switch_to_fast_model`) self-serialize via async mutex. Pi's `toolExecution: 'parallel'` stays — mutex wraps stateful execute() functions in `buildPiTools()`.

### Medium Value

- [x] **Retry with fallback model** — `src/agent/retry.ts` with exponential backoff + jitter. Wired into runner.ts for agent.prompt() and agent.continue().
- [x] **Deferred tool loading** — Deferred tools (`web_search`, `fetch_url`, `compare_versions`) have stub descriptions. `tool_search` meta-tool discovers full descriptions + parameters. 23 tools total.
- [x] **Input validation phase** — `src/tools/validators.ts` with `makeTool()` wrapper in `piToolAdapter.ts`. All 23 tools validated before execute(). Rejects bad LLM arguments before I/O.

### Tool-Specific Upgrades (from Claude Code's implementations)

#### fetch_url / web_search

- [x] **LRU cache with TTL on fetch_url** — `src/tools/web/fetchCache.ts`: 50MB LRU with 15-min TTL. Returns `fromCache: true` on cache hits.
- [x] **HTML→Markdown via Turndown** — Lazy-loaded Turndown in `fetchUrl.ts` with `htmlToText()` fallback. Preserves headings, lists, code blocks, links.
- [x] **Response size cap** — 10MB guard in `src/tools/web/fetchUrl.ts` via Content-Length check before reading body.
- [x] **Redirect safety** — `safeFetch()` in `fetchUrl.ts`: manual redirect with max 10 hops, cross-host redirects return `{ redirectedTo: url }` instead of following.
- [x] **Domain blocklist preflight** — `src/tools/web/domainBlocklist.ts`: blocks localhost, private IPs, AWS metadata, .local/.internal TLDs. SSRF protection.
- [x] **Expand approved sources list** — 17 domains in `approvedSources.ts`. Added TypeScript, Tailwind, GraphQL, Vercel, MDN, Stack Overflow, GitHub docs.

#### grep_pattern

- [x] **Ripgrep integration** — `src/tools/search/grepPattern.ts` tries `rg` first with JSON output, falls back to optimized Node.js walker with `readdir({ withFileTypes: true })`.
- [x] **Pagination (offset + head_limit)** — `offset` + `maxResults` with `truncated: boolean` in `grepPattern.ts`. Works in ripgrep and Node.js fallback.
- [x] **Output modes** — `files_with_matches` (file paths only) and `count` (per-file match counts) alongside `content` mode. Cheaper for broad searches.
- [x] **Sort by mtime** — Opt-in `sortByMtime: boolean` parameter. Stats unique files (capped at 200), sorts most-recent-first.
- [x] **Multiline pattern support** — `multiline: boolean` flag maps to ripgrep `-U --multiline-dotall`. Node.js fallback uses regex `gs` flag.

#### read_file / read_files_batch

- [x] **Read deduplication** — `fileReadCache` in AgentState with mtime + SHA-256 hash of first 1KB. Returns `unchanged: true` stub on cache hit. Works for `read_file` and `read_files_batch`.
- [x] **Line-range reads (offset + limit)** — `startLine` (1-based) in `resolveAndRead.ts`. Shows `(showing lines X-Y of Z total)` annotation.
- [ ] **TODO: Shared htmlToMarkdown utility** — If a second Turndown consumer appears, extract lazy-loaded Turndown into a shared utility.
- [x] **Binary file detection** — Extension check + null-byte scan of first 8KB in `src/tools/utils/resolveAndRead.ts`.

#### Cross-cutting tool patterns

- [x] **ENOENT → suggest similar files** — Levenshtein-based suggestions in `src/tools/utils/resolveAndRead.ts`. Top 3 matches with distance ≤ 3.
- [x] **Per-tool result size limits** — Done as part of tool result disk overflow (see above).
- [x] **Relative paths in all output** — Audited all 12 tools. All use `path.relative(repoRoot, ...)` for output paths. No absolute path leaks found.

### Low Value (Park for Later)

- [x] **Session cost persistence** — `src/output/sessionCosts.ts` with JSONL append, load, and aggregate. Wired into runner.ts post-run. 6 tests passing.
- [ ] **Generator-based agent loop wrapper** — Wrap Pi's callback events in an async generator for cleaner streaming, checkpointing, and composable middleware. Big refactor, defer until Pi's event model becomes a bottleneck.

## Eng Review (2026-04-02)

- [x] **Unit tests for extracted CLI commands** — 23 tests across `test/commands/analyze.test.ts` and `test/commands/compare.test.ts`. Covers argument validation, exit codes (green/yellow/red/error), dry-run, JSON mode, GitHub output hooks, and config passthrough.
- [x] **Silent error catches in dashboard** — `loadPersistedRuns()` now handles errors per-file with `console.warn` so one corrupt JSON doesn't nuke history. Added logging to `loadRunEvents()`, session restore, and DELETE calls.
- [x] **Type safety in recordFinding.ts** — Replaced all `as unknown as Finding` casts with `buildFinding()` that constructs type-safe Finding objects. Includes `toEvidence()` and `toDocRef()` validators for nested fields with safe defaults.

## CEO Demo Readiness Audit (2026-04-02)

- [x] **recordFinding type cast regression** — `6b47ed4` made `recordFinding` async for evidence verification but used `input as Record<string, unknown>` which fails strict TS. Caused 10 test failures and blocked dashboard production build. Fixed: `as unknown as Record<string, unknown>` in `8351443`.

## /autoplan Review (2026-04-03)

### Security (do these before anything else)

- [x] **fetch_url redirect safety** — `safeFetch()` with cross-host redirect blocking + `domainBlocklist.ts` with private IP/metadata endpoint protection. **SECURITY** (fixed in tool upgrades PR)

### Correctness (fix before next eng review)

- [x] **Fix broken READ_ONLY classification** — Moved `webSearchCount++` and `urlFetchCount++` from execute() to afterToolCall in runner.ts (race-free: beforeToolCall checks, afterToolCall increments). `filesRead.add()` stays in execute() because `recordFinding` checks it inside its own execute() and would race with afterToolCall. Updated comment in `concurrency.ts` to explain the distinction. `src/tools/piToolAdapter.ts`, `src/tools/concurrency.ts`, `src/agent/runner.ts`. (P1, S effort)
- [x] **Add enforcement for STATEFUL_TOOLS classification** — Added assertion loop at bottom of `buildPiTools()` that warns via `console.warn` for any tool not classified as read-only or stateful. `src/tools/piToolAdapter.ts`. (P1, XS effort)
- [x] **Fix double assemble_output silent overwrite** — Added guard: if `assembledRef.sections !== null`, logs `[assemble_output] Called twice — overwriting previous sections`. `src/tools/piToolAdapter.ts`. (P2, XS effort)

### Test gaps (from /autoplan)

- [x] **Strengthen concurrency test: verify mutual exclusion** — Replaced weak "both succeed" test with one that uses `StatefulToolMutex` directly + an activeCalls counter to prove maxConcurrent = 1. Also added classification assertion checking all 3 stateful tools are registered. `test/tools/piToolAdapter.test.ts`. (P2, XS effort)
- [x] **Test double assemble_output** — Added test that spies on `console.warn` and verifies the guard fires when assemble_output is called twice. `test/tools/piToolAdapter.test.ts`. (P2, XS effort)
- [x] **Add comment explaining per-call mutex isolation** — Added comment above mutex creation in `buildPiTools()` explaining each call gets its own independent mutex (correct for isolation between agent runs). `src/tools/piToolAdapter.ts`. (P3, XS effort)

### Decision Audit Trail

<!-- AUTONOMOUS DECISION LOG -->
| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
|---|-------|----------|---------------|-----------|-----------|---------|
| 1 | CEO | Mode: SELECTIVE EXPANSION | Mechanical | P3 | Maintenance pass post-CEO plan completion | EXPANSION (scope is complete) |
| 2 | CEO | Commit uncommitted changes | Mechanical | P6 | Complete, tested, 4 files, 70 lines, 269/269 pass | Defer (no reason to wait) |
| 3 | CEO | Approach C+B sequence (security fix → product validation) | Taste | P4 | Real SSRF vulnerability before more infrastructure | Approach A (ignore security finding) |
| 4 | Eng | Fix assemble_output silent overwrite: add warning log | Taste | P5 | Explicit over silent — at least log the scenario | Return error (breaks backward compat) |
| 5 | Eng | Add enforcement mechanism for STATEFUL_TOOLS | Mechanical | P1 | Completeness: prevent silent misclassification of new tools | Defer (no new tools planned now) |
| 6 | Eng | Fix piToolAdapter mutual exclusion test | Mechanical | P5 | Current test proves nothing about serialization | Skip (low priority) |
| 7 | Eng | Fix budget counter race (increment in execute vs beforeToolCall) | Mechanical | P4 | Check-then-act race can exceed budget under parallel tool firing | Defer |
| 8 | Eng | Add comment for per-call mutex isolation | Mechanical | P5 | Implicit: second buildPiTools() call gets independent mutex | Skip |
| 9 | Eng | Add 4 missing concurrency tests | Mechanical | P1 | Serialization verification, double assemble_output, budget race, enforcement | Defer |

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 5 | issues_open (via /autoplan) | 3 proposals deferred, 1 critical gap (product validation) |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 4 | issues_open (via /autoplan) | 6 issues, 3 critical gaps (SSRF, budget race, silent overwrite) |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAN | score 3→8/10, 11 decisions |

**ENG:** 3 critical gaps: (1) SSRF in fetch_url cross-host redirect, (2) budget enforcement race condition, (3) double assemble_output silent overwrite.
**CROSS-MODEL:** CEO + Eng both flagged fetch_url completeness as P1 independently. High-confidence signal.
**UNRESOLVED:** 3 (taste decisions: READ_ONLY classification fix approach, dashboard scope, backlog priority)
**VERDICT:** CEO + ENG open issues — commit concurrency.ts first, then fix SSRF + budget race before next eng review.
