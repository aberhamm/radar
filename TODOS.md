# TODOs — repo-audit-delivery-agent

Generated by /plan-eng-review on 2026-03-31. Track what's deferred.

## Demo Blockers (do these)

- [ ] **Chunk 0: Pi API spike** — Verify Pi import paths, tool registration, agent loop, termination. If Pi unavailable, fall back to DirectLoopRunner.
- [ ] **Chunk 0: Portkey verification** — Confirm Bedrock model ID `us.anthropic.claude-sonnet-4-20250514-v1:0` works. Fallback: `anthropic.claude-3-5-sonnet-20241022-v2:0`.
- [ ] **Chunk 0: Portkey cache token fields** — Does Portkey surface Bedrock's `cache_creation_input_tokens` / `cache_read_input_tokens` in the OpenAI-compatible usage response? If not, RunMetrics.cachedTokens defaults to 0. (Added by eng review #2)
- [ ] **Chunk 0: Pi mid-loop interruption** — Can the Pi agent loop be interrupted for budget extension prompts and interactive mode event callbacks? If not, both features use DirectLoopRunner. (Added by eng review #2)

## Pre-Demo (before first presentation)

- [ ] **Model pricing config** — Create `config/model-pricing.json` with Bedrock per-token prices for Sonnet 4.6 and Haiku 4.5. Used by RunMetrics for cost estimates. Falls back to token counts only if missing. Keep updated when pricing changes. (P2, S effort)
- [ ] **Pre-generated demo fixtures** — Run the agent against both target repos and save output to `output/demo-fixtures/`. Use for instant comparison demo and stable e2e test baselines. Regenerate before each demo. (P2, S effort)

## Post-Demo (not blocking)

- [ ] **Secret redaction** — Tools currently return raw env values. Add regex-based redaction for KEY, SECRET, TOKEN, PASSWORD patterns before returning tool output.
- [ ] **Optimizely rules** — `platform-optimizely.md` is a stub. Flesh out with real Optimizely SaaS patterns when needed.
- [ ] **Web search / fetch_url** — Currently stubbed. Implement with a real search API when needed for production use.
- [ ] **analyze_middleware** — Lower priority tool. Implement if time permits, skip for demo.
- [ ] **E2e tests** — Full agent loop against fixture repo. Assert 12 sections, scored scorecard, evidence-backed findings. Part of build, not deferred.
- [ ] **CLI polish** — Flags, help text, options. Phase 4 of spec.
- [ ] **HTML investigation log** — Render the agent's observe/reason/act log as browsable HTML.
- [ ] **JSON export** — Full agent state export for programmatic consumption.
- [ ] **Prompt injection defense** — Sanitize tool outputs before passing to LLM context. Strip instruction-like patterns from code comments and markdown in target repos. Important for untrusted repos in production. Security theme impresses demo audience too. (P3, M effort)
- [ ] **CI/CD goal type** — `goal-ci-check.md` + compact output format for PR comments. (NOW IN DEMO SCOPE per CEO review)
